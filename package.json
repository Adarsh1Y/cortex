{
  "name": "cortex",
  "version": "0.1.0",
  "private": true,
  "description": "CORTEX - consciousness layer for CLI AI agents: persistent memory, personality, and presence",
  "type": "module",
  "workspaces": [
    "core",
    "shell"
  ],
  "scripts": {
    "dev": "bun run shell/repl.ts",
    "web": "bun run shell/web.ts",
    "test": "pkill -f \"opencode serv[e] --hostname\" 2>/dev/null; sleep 1; bun test test/ --parallel=1 --timeout 10000",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@opencode-ai/sdk": "latest"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
